# Keep create-drop until you are certain everything is working correctly
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.database=mysql
spring.jpa.show-sql=true

spring.datasource.url=jdbc:mysql://localhost:3306/winecellar?useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=wine
# Change the password before using jasypt, then encrypt
spring.datasource.password=password